/*public class ActualDriver {
// http://stackoverflow.com/questions/1238145/how-to-run-a-jar-file
    public static void main(String[] args) {
        // Scheduler is started by providing where to obtain files -
        String nodeType = args[0];
        int heartBeatFrequencyInMinutes = args [1]
        if (nodeType.equals("s")) {
            // args[2] - List of workers
            // args[3] - List of reducers
            // args[4] - Directory w. tasks
        }

else {
            String schedulerIpPort = args[2];
            SchedulerInfo scheduler = new SchedulerInfo();
            if (nodeType == "w") {
                new Worker()
            }

        }

            String
            new SchedulerInfo()
        }
}
*/